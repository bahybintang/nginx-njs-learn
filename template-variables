map $host $upstream_domain {
  default "$UPSTREAM_DOMAIN";
}

map $host $upstream_enable_ssl {
  default "$UPSTREAM_ENABLE_SSL";
}

map $host $enable_ssl {
  default "$ENABLE_SSL";
}

map $host $domain {
  default "$DOMAIN";
}